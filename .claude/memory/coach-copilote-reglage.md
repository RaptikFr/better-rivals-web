---
name: coach-copilote-reglage
description: Copilote de réglage (brique télémétrie
metadata: 
  node_type: memory
  type: project
  originSessionId: b2b7fdc8-c25c-475f-8715-03ae17566fd0
---

Chantier **copilote de réglage** (brique télémétrie #5). Outils LOCAUX dans `C:\Users\night\OneDrive\Relais\` (gitignorés, hors repo web) :
- `telemetrie_debug.py` : capture/valide les offsets UDP FH6, écrit `.bin` (paquet = longueur `<H` + données brutes).
- `coach_diag.py` : prototype HORS-LIGNE qui rejoue un `.bin` et sort calibration + diagnostic par virage + ancrage secteur + pistes de réglage. Mode `compare avant.bin apres.bin`. Sauvegarde : `coach_diag.bak.py`.

**Offsets confirmés en jeu (21/06)** : bloc sled 0–231 NON décalé, bloc dash ≥232 décalé **+12**. Canaux clés : ax=20 az=28 (accél lat/long), velX=32 velZ=40, yaw=48, susp norm=68/72/76/80 (FL FR RL RR, 0=détendue 1=comprimée), slip_ratio=84/88/92/96, slip_angle=164/168/172/176 (rad), speed=256, accel=315 brake=316 gear=319 steer=320, dist=292, cur_lap=304, drivetrain=224 (0=FWD 1=RWD 2=AWD).
**RPM confirmé le 22/06** (format sled standard Forza = aucun lien externe nécessaire, validé sur captures `.bin`) : **RPM_MAX=8** (rupteur, ~constant), **RPM_IDLE=12** (ralenti, ~constant), **RPM_CUR=16** (courant, varie 800↔9000). Ajoutés au décodeur coach_diag + documentés dans telemetrie_debug.py FIELDS.

**Doc officielle FH6 obtenue le 22/06** (le proprio a collé le texte ; le site support.forza.net renvoie 403 au fetch). Paquet **324 octets, format FIXE** (pas de choix sled/dash comme en FM). Le **+12 du bloc dash s'explique** : 3 champs FH6 nouveaux insérés après NumCylinders (offset 228) avant PositionX → **CarGroup U32 @232, SmashableVelDiff F32 @236, SmashableMass F32 @240**. Tous nos offsets validés collent exactement à la doc. **TireTemp (°F) FL/FR/RL/RR = 268/272/276/280** confirmé sur captures (med ~150-160°F, AV ~+6°F vs AR). ⚠️ **FH6 N'A PAS** TireWear ni TrackOrdinal, ni aucun champ de pression pneu ; température = 1 seule valeur par pneu (pas de gradient intérieur/extérieur) → **carrossage & usure & pression-précise INDIAGNOSTICABLES**. SmashableVelDiff=0 sur les captures (smashables = décors cassables, pas murs/bordures) → n'aide pas au filtrage bordure ; ajouté quand même à `est_suspect` (>0,5 = choc franc).

**Levier TEMPÉRATURE ajouté le 22/06** (`analyser_temp`, `diagnostic_temp`) : médiane par pneu, AV vs AR = 4e juge d'équilibre INDÉPENDANT (l'essieu le plus chaud travaille le plus). Sert de CORROBORATEUR : le conseil **antiroulis (sous-virage) sort désormais** si l'avant chauffe nettement plus (Δ>SEUIL_TEMP_DELTA=12°F, à calibrer) ET slip-angle penche sous-virage — ça débloque enfin le levier équilibre qui était mort. + détection surchauffe relative d'un pneu (>18°F au-dessus de la moyenne → pression à chaud plus basse). Intègre le freinage (avant) → corroborateur, pas juge absolu. Sur capture test : Δ+7°F → « proches » → pas de faux conseil.

**Logique d'équilibre = 3 juges** : slip-angle AV/AR (biaisé : le slip-angle AV inclut la géométrie de braquage → sur-déclare le sous-virage), contre-test lacet (C=médiane yaw/(v·steer) en bas régime), et **β = angle de dérive caisse atan2(velX,velZ)** = juge ROBUSTE (ne détecte que le survirage/glisse). Règle prudente : conseil de sous-virage seulement si slip-angle ET lacet d'accord ; survirage piloté par β.

**Fiabilisation faite le 22/06** (validée sur captures, PAS encore en jeu) : (1) filtrage des frames polluées `est_suspect()` — |ax|/|az|>30 m/s² ou slip-angle>0,6 rad = bordure/choc/tête-à-queue (~15 % des frames !) ; (2) segmentation à HYSTÉRÉSIS (entrée 4.0, sortie 2.5 m/s²) + FUSION des zones proches (un virage haché = 1 virage) ; (3) équilibre évalué sur le RÉGIME ÉTABLI (frames ≥0,7×g_max, près de l'apex) au lieu de toute la durée. Résultat : 82→52 virages, faux survirages de bordure éliminés, verdict prudent « voiture neutre ». RESTE : valider en jeu qu'une vraie voiture sous-vireuse est détectée (calibrer SEUIL_BALANCE/U).

**⚠️ Honnêteté sur l'état des leviers** (le proprio a corrigé une surestimation de ma part) : un conseil n'existe que s'il SE DÉCLENCHE vraiment, pas s'il est juste écrit dans un texte. Réellement actifs : **frein** (blocage AV/AR → balance, le seul qui sort souvent), **diff accél / survirage** (seulement si β voit une glisse), **ressorts/hauteur** (seulement via talonnage). L'**antiroulis** n'est mentionné que dans la branche « sous-virage confirmé » qui exige slip-angle ET lacet d'accord → ne sort JAMAIS (juges anti-corrélés). La pression pneus = même branche morte. Donc l'équilibre (antiroulis/ressorts) est de fait BLOQUÉ tant que les juges ne sont pas validés en jeu.

**Leviers amortisseurs + aéro AJOUTÉS le 22/06** dans coach_diag.py : 
- `analyser_amortisseurs` : compte les inversions de débattement/s par essieu (hystérésis MIN_OSC_AMPL). NIVEAU absolu NON calibré → conseil émis SEULEMENT sur déséquilibre AV/AR (>1,4×). Sur la capture test : AV 7,2 ≈ AR 7,1 → aucun conseil (bien).
- `analyser_aero` : équilibre virages lents vs rapides, découpage ADAPTATIF (médiane des vitesses de virage) + garde d'écart ≥8 m/s ; comparaison RELATIVE du ratio slip-angle (annule le biais constant). β en virage rapide = appui AR. Sur capture test : ratios 1,23 vs 1,18 → « appui cohérent ».
Les deux demandent une CALIBRATION en jeu (rouler soft/stiff dampers, plus/moins d'aileron) avant de durcir les seuils.

**Port relais `coach_reglage.py` LIVRÉ** (présent dans `OneDrive\Relais`, daté 23/06) : version COMPACTE de coach_diag (mêmes juges/seuils) qui sort `diagnostic_compact()` → `{titre, conseils[:2]}` pour l'overlay en jeu. Pur, sans dépendance, bundlé par PyInstaller et importé en best-effort dans le relais (`import coach_reglage`, désactivé si absent). Intégré au relais **v300 / APP_VERSION 3.0.0** (« copilote de réglage en jeu » — voir [[relais-serveur-et-rang]]).

**Captures de calibration RÉALISÉES par le proprio + analysées le 23/06** (`coach_diag.py <fichier>.bin` sur neutre/sousvireuse/survireuse/amorto_mou/amorto_dur/aero_min/aero_max). ⚠️ **Le journal « vérité terrain » du PROTOCOLE est resté VIDE** (aucun ressenti noté). Résultats bruts :
- `neutre` → verdict neutre ✅ (Δtemp AV/AR +6 °F, β absent).
- `sousvireuse` (ARB AV dur attendu) → **AUCUN conseil de sous-virage ❌** : Δtemp +6 (= neutre), slip-angle ne franchit pas le seuil. Le levier équilibre n'a PAS détecté la voiture sous-vireuse.
- `survireuse` (ARB AR dur attendu) → **neutre ❌** : Δtemp −0, β absent, pas de glisse.
- `amorto_mou` → σ débattement **0.18** (vs 0.13 neutre) + **talonnage** détecté, MAIS AV/AR équilibrés → aucun conseil amortisseur (la règle « déséquilibre AV/AR seulement » étouffe le signal de NIVEAU, pourtant présent).
- `amorto_dur` → σ ~0.11, équilibré, aucun conseil.
- `aero_min`/`aero_max` → **les DEUX** : AV surchauffe nettement (Δ +27 / +21 °F) → diagnostiqués sous-virage ; mais le levier aéro (ratio lents vs rapides) **ne sépare PAS** min de max (ratios quasi identiques ~0.98-0.99 en rapide). Captures faites sur un circuit/voiture différents (front très chargé).
**⚠️ CORRECTION (même jour) : ces résultats sont ceux du RAPPORT SIMPLE, qui n'émet un conseil que sur déséquilibre AV/AR ou seuil absolu → trompeur pour un changement symétrique. Le bon outil pour juger un réglage = le mode `compare avant.bin apres.bin`.** En `compare`, les offsets d'hier (temp 268-280, susp 68-80) détectent bien 2 des 3 :
- **amortisseurs** ✅ net : `compare neutre→amorto_mou` = +0,8 osc/s, σ +0,052 « caisse plus FLOTTANTE (sous-amortie) » ; `mou→dur` = −3,2 osc/s « plus POSÉE ». σ mou 0,182 vs neutre 0,130 vs dur 0,155 — discriminateur fiable EN RELATIF (= le « +40 % » noté dans le code).
- **survirage** ✅ détecté : `compare neutre→survireuse` = Δtemp AV−AR +8 → +2 (**−5 °F**) « l'ARRIÈRE travaille +, plus survireur ».
- **sous-virage** ⚠️ FAIBLE : `compare neutre→sousvireuse` = Δtemp +8 → +8 (thermiquement PLAT) ; seul β baisse (1,65→1,19). C'est la SEULE capture à reprendre : soit ARB AV pas assez tranché, soit le sous-virage gratte la vitesse sans chauffer l'avant sur cette voiture/circuit.
Bonus : les 3 réglages poussés sont plus LENTS au chrono (+1,07s sousvireuse, +0,69s survireuse) → l'outil conseille « reviens au neutre » = cohérent.
**⚠️ CALIBRATION CLOSE le 10/07/2026** : le proprio ne refera PAS de captures de calibration ni le journal ressenti (« au pire j'utiliserais mes .bin pour des analyses et peut-être modifier le copilote, mais c'est tout »). Ne plus proposer de retest sousvireuse ni de remplir le journal — le sous-virage est de toute façon confirmé en conditions réelles (09/07, Δtemp +31 °F). Usage futur des .bin = analyses ponctuelles à la demande. **Piste code** : faire remonter le signal de NIVEAU (compare/ancre) dans le rapport simple/overlay, pour ne plus rester muet sur un réglage symétrique.

**Capture technique brute v3.2.0 TESTÉE EN JEU le 09/07** (confirmé par le proprio) : `captures_perso/` se remplit bien pendant les sessions réelles (5 fichiers, 3 voitures, 07/07). `coach_diag.py` lit ces `.bin` sans souci — le format est validé de bout en bout en conditions réelles (pas seulement `py_compile`).

**🐛 BUG balance de frein CORRIGÉ le 09/07** (trouvé en analysant les 5 captures du 07/07) : `coach_reglage.py` (compact, overlay en jeu) testait le blocage AVANT en premier (`if`/`elif`) → dès qu'il dépassait 10%, l'ARRIÈRE n'était plus jamais regardé, même s'il bloquait davantage. Sur `course_20260707_220230_3767.bin` : AV 17,5% / AR 22,3% (l'arrière bloque LE PLUS) → l'overlay disait quand même « vers l'arrière » (repose sur l'avant, qui n'est pourtant pas le plus gros problème) — recommandation **inversée**. `coach_diag.py` (rapport complet) avait la variante inverse : deux `if` indépendants → sortait les DEUX conseils contradictoires sans dire lequel prime, quand les deux essieux dépassent 10% en même temps.
Fix (les deux fichiers) : comparer les magnitudes `bloque_av/au_frein` vs `bloque_ar/au_frein`, ne recommander QUE l'essieu dominant. Vérifié sur les 5 captures du 07/07 : `220230_3767` et `221756_3767` (où l'arrière domine légèrement) donnent maintenant la bonne direction ; les 3 autres (avant nettement dominant) restaient déjà corrects.
**Nom des fichiers `captures_perso/` enrichi (release v3.2.2, 09/07)** : `RawCaptureRecorder.demarrer()` construit désormais `#<car_ordinal>_<classe>_<transmission>_<ddmmyy>_<hhmmss>.bin` (ex. `#3569_S2_AWD_080726_213548.bin`) au lieu de `course_<date>_<ordinal>.bin` — classe/transmission décodées depuis `fields` via `CAR_CLASSES`/`DRIVETRAINS` (déjà dispo à l'appel, aucun round-trip API). Identifie la config sans ouvrir le fichier. Banner du site PAS rebump (id inchangé) : changement invisible pour les comptes non whitelistés, pas besoin de rouvrir la bannière pour tout le monde — seul le texte a été mis à jour au cas où elle se réafficherait.

**Nom du CIRCUIT ajouté (release v3.2.3, 09/07)** : `#<ordinal>_<classe>_<transmission>_<circuit-slug>_<ddmmyy>_<hhmmss>.bin` (ex. `#3569_S2_AWD_circuit-de-lautoroute_080726_213548.bin`). Le proprio pensait qu'il fallait attendre que TOUS les circuits aient leur `track_geometries` avant de pouvoir faire ça — **faux** : `self.session["track_name"]` est déjà résolu au moment où la capture démarre, que ce soit par sélection manuelle (toujours connu, zéro dépendance à `track_geometries`) OU par auto-détection (qui ne verrouille QUE sur un circuit dont le tracé est déjà en base — donc si elle a matché, le nom est par construction déjà connu). Nouvelle fonction pure `_slug_nom()` (accents/apostrophes/espaces → kebab-case ASCII, `unicodedata` + `re`) pour un nom de fichier Windows-safe. État de la couverture `track_geometries` au 09/07 : 9/86 circuits approuvés seulement (la plupart des sprints/cross-country/trail n'ont de toute façon pas cette feature, gatée à l'asphalte).

**✅ DÉPLOYÉ le 09/07** : rebuild + release GitHub **v3.2.1**, **v3.2.2**, puis **v3.2.3** publiée (`relais_gui_v312.py`, APP_VERSION bumpé, `coach_reglage.py` corrigé bundlé par PyInstaller). Build fait via `pyinstaller.exe` direct (le module `pyinstaller` n'était pas trouvé par `python -m pyinstaller`, mais l'exe dans `Scripts/` fonctionne). Release publiée via l'API GitHub en Python/requests (pas de `gh` sur le portable) avec le token récupéré via `git credential fill` (déjà en cache Windows Credential Manager depuis un push précédent — pas de PAT dédié nécessaire). `/telecharger` et la bannière d'annonce du site bumpés en v3.2.1, commit poussé.

**Sous-virage CONFIRMÉ en conditions réelles (analyse du 09/07, capture `course_20260707_225315_3134.bin`, FWD, 4,66 km, 37 virages)** : signal net et CONVERGENT cette fois — Δtemp AV−AR = **+31 °F** (AV 176 °F vs AR 144 °F) ET slip-angle sous-virage. Contrairement au test calibré du 23/06 (Δ+8°F, thermiquement plat), ce test « en conduite normale » montre que le juge température/slip-angle fonctionne bien quand le sous-virage est réellement marqué — le résultat plat du 23/06 tenait probablement à un réglage ARB pas assez extrême sur cette capture-là, plutôt qu'à un défaut de la logique. Le retest contrôlé ARB-aux-butées reste utile pour une confirmation strictement comparable, mais n'est plus bloquant.
La capture RWD (4158) du même lot ressort neutre (Δ+1°F, β plat) — cohérent, pas de faux positif.

**Diff DÉCÉLÉRATION + pression de frein AJOUTÉS le 22/06** (`analyser_entree`, `diagnostic_entree`) : isole le trail-braking (frein>40/255 + |volant|>12 + v>8 m/s) et compte les frames en glisse arrière (β) → entrée survireuse = diff décel trop ouvert/arrière trop libre/frein trop AR. Pression de frein : si blocage AV+AR ≥30 % du freinage → réduire la pression globale. Ajouté aussi au mode `compare` (ligne « Entrée survireuse % »). Sur capture test : 104 frames trail-braking, 0 glisse → « entrée stable » (pas de faux positif).

**BOÎTE AJOUTÉE le 22/06** (`analyser_boite`, `diagnostic_boite`) : à plein gaz (accel>220), regarde le RPM dans le DERNIER rapport utilisé. Rupteur tapé (RPM≥98,5 % max) >10 % du temps → rapport final trop COURT (allonger) ; pic <90 % du rupteur → trop LONG (raccourcir, si ligne droite assez longue). Sur capture test : dernier rapport à 94 % → « cohérent », pas de faux conseil.

**Seuls leviers restants NON faisables** (confirmé par la doc officielle) : ❌ carrossage (besoin gradient de temp sur la largeur du pneu, absent), ❌ usure pneu (champ TireWear absent de FH6), ❌ pression précise (aucun champ pression ; on n'a que la temp). La temp pneu donne au mieux un proxy de pression « à chaud » par surchauffe relative. TOUS les autres sont codés : équilibre/antiroulis (bloqué calibration juges), frein balance+pression, diff accél+décél/entrée, ressorts/hauteur (talonnage), amortisseurs, aéro, boîte. **En attente de CALIBRATION en jeu** (captures de référence à comportement connu) pour durcir les seuils — sauf frein/diff/boîte qui sont déjà fiables (pilotés par β ou RPM). **Protocole détaillé** dans `C:\Users\night\OneDrive\Relais\PROTOCOLE_CALIBRATION_COPILOTE.md` (OneDrive, hors git) : captures `neutre/sousvireuse/survireuse/amorto_mou/amorto_dur/aero_min/aero_max.bin` + journal de vérité terrain à remplir. Méthode : 1 voiture réglable + 1 circuit, 1 paramètre à l'extrême par capture. Cf. [[feature-trace-telemetrie]] [[feature-secteurs]] [[relais-serveur-et-rang]].
